ショッピングカート
<%= link_to "カートを空にする", cart_items_destroy_all_path, class:"btn btn-sm btn-danger", method: :delete, "data-confirm" => "本当に消しますか？"   %>
<% @cart_items.each do |cart_item| %>
商品名<%= cart_item.item.name %>
単価(税込)<%= cart_item.item.add_tax_price.to_s(:delimited) %><td>  <%= cart_item.item.with_tax_price.to_s(:delimited) %></td>
<%= form_with model: cart_item, url: cart_item_path(cart_item), method: :patch do |f| %>
数量<%= f.select :amount, *[1..10] %>
<%= f.submit '変更', class:"btn btn-success" %>
<td><%= cart_item.subtotal %></td>
<% @total +=  cart_item.subtotal %>
<% end %>
小計
<%= link_to "削除する", cart_item_path(cart_item), class:"btn btn-sm btn-danger" , method: :delete, "data-confirm" => "本当に消しますか？"  %>
<% end %>
<%= link_to "買い物を続ける", items_path, class:"btn btn-primary" %>
合計金額
<%= @total.to_s(:delimited) %>
<%= link_to '情報入力に進む', new_order_path, class:"btn btn-success" %>