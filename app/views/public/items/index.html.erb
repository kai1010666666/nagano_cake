商品一覧(全<%= @items.count %>件)
<% @items.each do |item| %>
<%= image_tag item.get_image(100,100) %>
<div><%= item.name %></div>
<div>￥<%= item.add_tax_price.to_s(:delimited) %></div>
<% end %>
<%= paginate @items %>